"""一次性文档对齐脚本：把 docs 里的数值与实现对齐（OQ-01 确认后）。

运行：python tools/align_docs.py
"""

from __future__ import annotations

import pathlib

ROOT = pathlib.Path(__file__).resolve().parent.parent


def patch(path: str, pairs: list[tuple[str, str]], required: bool = True) -> None:
    file = ROOT / path
    text = file.read_text(encoding="utf-8")
    for old, new in pairs:
        if old not in text:
            if required:
                raise SystemExit(f"[{path}] 未找到待替换片段：\n{old[:160]}")
            continue
        text = text.replace(old, new)
    file.write_text(text, encoding="utf-8")
    print(f"  ok  {path}")


# --------------------------------------------------------------------------- #
# docs/04 · 评分与证据链
# --------------------------------------------------------------------------- #
patch("docs/04-评分与证据链.md", [
    (
        "> **待用户确认项（OQ-01）**：是否接受「以 §12 权重为准、§13/§14 仅作展示参考」的处理。\n"
        "> 若希望严格复现 §13 的 `+25/+20/...` 数值，需要放弃 §12 的权重表 —— 两者不可兼得。",
        "> **OQ-01（已确认 2026-09-11）**：**以 §12 权重为准，§13 / §14 / §17 仅作展示形态参考。**\n"
        "> 规范算例的可复算结果为 `rule_score = 67.5875（显示 68）`，见 §8。\n"
        "> 若将来希望严格复现 §13 的 `+25/+20/...` 数值，必须放弃 §12 的权重表 —— 两者不可兼得。",
    ),
    (
        "| `R-GEN-EV-04` | 事件牵涉知名产业方 / 头部企业 | +10 |\n"
        "| `R-GEN-EV-05` | 仅由 C 类媒体报道，无正式公告 | +10 |\n"
        "| `R-GEN-EV-06` | 仅由 E 类市场讨论 | +5 |\n"
        "| | 上限 | 100 |",
        "| `R-GEN-EV-04` | 事件牵涉知名产业方 / 头部企业 | +10 |\n"
        "| `R-GEN-EV-05` | 仅由 C 类媒体报道，无正式公告 | +10 |\n"
        "| `R-GEN-EV-06` | 仅由 E 类市场讨论 | +5 |\n"
        "| | 求和上限 | 100 |\n"
        "\n"
        "> **注意**：本维度**不再单独设「时效加成」项**。时效通过 §5 的衰减系数作为\n"
        "> **乘数**作用在维度原始分上；若再加一个时效加减分项，同一件事会被计两次。\n"
        "> 因此 `R-GEN-EV-TIME` / `R-GEN-MA-TIME` 只在拆解里显示衰减系数（`delta = 0`），\n"
        "> 用于向用户解释「为什么这条很新的事件分数却被压低了」。",
    ),
    (
        "| 风险因素 | 权重 | 严重度判据 |\n"
        "|---|:---:|---|\n"
        "| 事件失败可能性（如重组被否/终止） | 0.25 | 历史失败记录、审批未完成、方案未定 |\n"
        "| 公司基本面恶化 | 0.20 | 连续亏损、现金流为负、退市风险 |\n"
        "| 监管 / 合规风险 | 0.20 | 问询未回复、监管函、立案调查 |\n"
        "| 估值风险 | 0.10 | 估值处历史高位（对价值策略权重上调） |\n"
        "| 大股东质押 / 资金占用 | 0.15 | 质押比例、占款 |\n"
        "| 流动性 / 可交易性 | 0.10 | 停牌、日均成交额过低 |",
        "**严重度是 0~1 的连续值**，由规则从事实推导（不是让 LLM 打分），形容词分档仅用于展示：\n"
        "\n"
        "| 分档 | 区间 | 分档 | 区间 |\n"
        "|---|:---:|---|:---:|\n"
        "| 高 | ≥ 0.80 | 中 | 0.40 ~ 0.60 |\n"
        "| 中高 | 0.60 ~ 0.80 | 中低 | 0.20 ~ 0.40 |\n"
        "| 低 | < 0.20 | | |\n"
        "\n"
        "**风险因素权重按策略定义**（`ThesisTypeDef.risk_factors`），因此不同策略的风险结构不同。\n"
        "以 `restructuring` 为例（10 类策略的完整风险定义见 `06-策略设计总表`）：\n"
        "\n"
        "| 风险因素 | 权重 | 基础严重度 | 推导方式（触发条件 → 严重度） |\n"
        "|---|:---:|:---:|---|\n"
        "| 事件失败可能性 | 0.30 | 0.30 | 历史同类事项失败 → 0.55；+后期阶段仍有审批未完成 → +0.15；已出现失效事件 → 1.00 |\n"
        "| 监管 / 合规风险 | 0.25 | 0.20 | 未回复的监管问询 → 0.45；+与公告口径不一致的媒体报道 → +0.10 |\n"
        "| 公司基本面恶化 | 0.20 | 0.20 | 连续 ≥2 年亏损 → 0.40；+经营现金流为负 → +0.15；**归因于一次性因素 → 覆盖为 0.20** |\n"
        "| 大股东质押 / 资金占用 | 0.15 | 0.20 | 高比例股权质押 → 0.35 |\n"
        "| 可交易性 | 0.10 | 0.20 | 停牌 → 0.80 |\n"
        "\n"
        "> **基础严重度的意义**：即使一条触发条件都没命中，风险也不为 0。\n"
        "> 「未知」按「存在基础风险」处理，而不是按「没有风险」处理 —— 避免乐观偏差。\n"
        "\n"
        "> **关于「同一事实出现在两个维度」**：`high_pledge` 同时影响\n"
        "> `SHAREHOLDER_STRUCTURE`（净值型：股东结构现状有多差）与 `RISK`（概率型：可能怎么坏）。\n"
        "> 两者回答**不同问题**，因此不算重复计分；被禁止的是「同一维度内对同一事实计两次」。\n"
        "> 同理，未回复的问询函同时降低 `CERTAINTY`（确认到什么程度）并抬高 `RISK`（爆发概率）。",
    ),
])
print("docs/04 主体已更新")


# --------------------------------------------------------------------------- #
# docs/04 §8 · 用自洽算例替换原算例
# --------------------------------------------------------------------------- #
CANONICAL = """## 8. 完整算例（自洽公式，可被单测复现）

> **本算例被 `backend/tests/test_scoring.py` 逐项锁定。** 规则改动导致这些数字变化时，
> 必须同步更新本节，并说明是规则语义变了还是权重变了。

**输入**：ST XXX，用户画像「重组预期 40% / 并购整合 25% / 困境反转 15%」
（归一化后最大权重 `w_max = 0.40`，故 `w_thesis / w_max = 1.0`）

事实：重大资产重组**预案**公告（A 级）+ 控股股东变更（A 级）+ 资产注入进展（A 级）；
ST 状态且连续 2 年亏损；6 项待确认事项；交易所问询未回复；2024 年曾有重组终止记录；
无质押、未停牌；出现异常波动公告与社交讨论。

**规则命中**

| 维度 | 规则命中 | raw |
|---|---|:---:|
| `THESIS_MATCH` | C1/C2/C3 完全命中 + C4 部分满足（0.70）→ coverage = 0.94 | **94.00** |
| `EVENT_CATALYST` | `R-GEN-EV-01` +40、`R-GEN-EV-02` +25、`R-GEN-EV-03` +15、`R-GEN-EV-04` +10 → 90，衰减系数 1.00 | **90.00** |
| `CATALYST_STRENGTH` | 预案披露阶段 → 40 | **40.00** |
| `CERTAINTY` | A 级基线 100；6 项待确认 −30（触顶）；未回复问询 −15；历史失败 −10 | **45.00** |
| `FUNDAMENTALS` | 基线 50；无改善项；无减值/应收/负债率扣分 | **50.00** |
| `SHAREHOLDER_STRUCTURE` | 基线 50；`R-GEN-SH-01` 控股股东变更 +40；无质押扣分 | **90.00** |
| `MARKET_ATTENTION` | 基线 40；`R-GEN-MA-02` 异动 +15；`R-GEN-MA-05` 社交讨论 +5 | **60.00** |
| `HISTORY_CASE` | 案例库未启用（Phase 2） | **0.00** |
| `RISK` | 0.30×0.55 + 0.25×0.45 + 0.20×0.40 + 0.15×0.20 + 0.10×0.20 = 0.4075 | **40.75** |

**加权（`restructuring` 的策略级覆盖后权重）**

```
THESIS_MATCH           94.00 × 0.30 = 28.2000
EVENT_CATALYST         90.00 × 0.25 = 22.5000
CATALYST_STRENGTH      40.00 × 0.10 =  4.0000
CERTAINTY              45.00 × 0.10 =  4.5000
FUNDAMENTALS           50.00 × 0.05 =  2.5000
SHAREHOLDER_STRUCTURE  90.00 × 0.10 =  9.0000
MARKET_ATTENTION       60.00 × 0.05 =  3.0000
HISTORY_CASE            0.00 × 0.00 =  0.0000
                                      ────────
正向合计                                 73.7000
risk_penalty = (40.75 / 100) × 15      = −6.1125
                                      ────────
rule_score                             = 67.5875  → 显示 68
```

**卡片展示**

```
ST XXX                        重组预期   94% MATCH
◉ 待确认                     机会分 68   风险 41
语义分 76 · 分歧 8.41（正常）
```

**与规格的差异说明**

| 出处 | 规格数值 | 处理 |
|---|---|---|
| §12 权重表 | — | **采纳为权威** |
| §13 逐项拆解 | 合计 79，却称最终 82 | 采纳其**展示形态**（两级展开），数值以本公式为准 |
| §14 维度分 | 按 §12 权重复算得 72.75 | 采纳其**展示形态**（维度条形图），数值以自洽公式为准 |
| §17 卡片 | 机会评分 82 | 按 §12 权重 + 自洽规则复算为 **68** |

> 四处数值不可能同时成立（见 §1）。本项目选择**可复算的 68**，而不是**无法复现的 82**。
> 这是 OQ-01 已确认的处理方式。

"""

file = ROOT / "docs/04-评分与证据链.md"
text = file.read_text(encoding="utf-8")
start = text.index("## 8. 完整算例")
end = text.index("## 9. 版本与重算")
file.write_text(text[:start] + CANONICAL + text[end:], encoding="utf-8")
print("  ok  docs/04 §8 算例已替换")


# --------------------------------------------------------------------------- #
# docs/05 · 接口示例中的分数
# --------------------------------------------------------------------------- #
patch("docs/05-API契约.md", [
    ('  "rule_score": 69,', '  "rule_score": 67.59,'),
    ('    "rule_score": 69,', '    "rule_score": 67.59,'),
    ('        "rule_score": 69, "risk_score": 43,',
     '        "rule_score": 67.59, "risk_score": 40.75,'),
    ('{ "rule_score": 69, "semantic_score": 76,', '{ "rule_score": 67.59, "semantic_score": 76,'),
    ('"risk_score": 43,', '"risk_score": 40.75,'),
], required=False)


# --------------------------------------------------------------------------- #
# docs/07 · 算例引用与 OQ-01 状态
# --------------------------------------------------------------------------- #
patch("docs/07-MVP实施计划.md", [
    ("| `test_scoring.py` | `04` §8 的算例必须复算出 `rule_score = 69` | M6-03 |",
     "| `test_scoring.py` | `04` §8 的算例必须复算出 `rule_score = 67.5875`（显示 68） | M6-03 |"),
    ("| R6 | 规格 §12/§13/§14 数值不自洽 | 算例无法复现 82（得 69） | **OQ-01 待你确认** | 以 §12 权重为准，§13/§14 作展示参考 |",
     "| R6 | 规格 §12/§13/§14 数值不自洽 | 算例无法复现 82（得 68） | OQ-01 已确认 | 以 §12 权重为准，§13/§14/§17 作展示参考 |"),
    ("`04` §8 算例得 **69** 而非 82", "`04` §8 算例得 **68** 而非 82"),
    ("| **OQ-01** |", "| **OQ-01** ✅ 已确认 |"),
], required=False)


# --------------------------------------------------------------------------- #
# docs/06 · 6 个策略的权重与股东行为风险
# --------------------------------------------------------------------------- #
patch("docs/06-策略设计总表.md", [
    ("  MARKET_ATTENTION: 0.05 · HISTORY_CASE: 0.10",
     "  MARKET_ATTENTION: 0.05 · HISTORY_CASE: 0.05"),
    (
        "  THESIS_MATCH: 0.30 · EVENT_CATALYST: 0.20 · CATALYST_STRENGTH: 0.10\n"
        "  CERTAINTY: 0.10 · FUNDAMENTALS: 0.10 · SHAREHOLDER_STRUCTURE: 0.05\n"
        "  MARKET_ATTENTION: 0.05 · HISTORY_CASE: 0.05",
        "  THESIS_MATCH: 0.30 · EVENT_CATALYST: 0.20 · CATALYST_STRENGTH: 0.05\n"
        "  CERTAINTY: 0.10 · FUNDAMENTALS: 0.10 · SHAREHOLDER_STRUCTURE: 0.05\n"
        "  MARKET_ATTENTION: 0.05 · HISTORY_CASE: 0.10   ← 权重交给历史并购表现",
    ),
    (
        "risk_factors:\n"
        "  增持规模过小（象征性）: 0.25 · 资金来源存疑: 0.25 · 大股东质押: 0.20\n"
        "  历史增持后表现差: 0.15 · 高负债: 0.15",
        "risk_factors:\n"
        "  增持 / 回购计划未实施或缩水: 0.20   # ★ §14.3：必须有「事件可能失败」类风险\n"
        "  增持规模过小（象征性）: 0.20 · 资金来源存疑: 0.20\n"
        "  大股东质押: 0.15 · 历史增持后表现差: 0.15 · 高负债: 0.10",
    ),
    (
        "  THESIS_MATCH: 0.30 · EVENT_CATALYST: 0.15 · CATALYST_STRENGTH: 0.10\n"
        "  CERTAINTY: 0.10 · FUNDAMENTALS: 0.10 · SHAREHOLDER_STRUCTURE: 0.20\n"
        "  MARKET_ATTENTION: 0.05 · HISTORY_CASE: 0.00",
        "  THESIS_MATCH: 0.30 · EVENT_CATALYST: 0.15 · CATALYST_STRENGTH: 0.05\n"
        "  CERTAINTY: 0.10 · FUNDAMENTALS: 0.10 · SHAREHOLDER_STRUCTURE: 0.20\n"
        "  MARKET_ATTENTION: 0.05 · HISTORY_CASE: 0.00",
    ),
])

# 在 docs/06 顶部补一条权重合计的硬约束说明
patch("docs/06-策略设计总表.md", [
    (
        "## 2. 策略一览",
        "> **权重硬约束**：每个策略的正向维度权重合计必须恰为 **0.95**（`RISK` 不在正向权重表内，\n"
        "> 而是以最高 15 分的扣分形式出现）。代码里 `_weights()` 在导入时就会校验，\n"
        "> 算错立即报错而不是静默漂移。\n"
        "\n"
        "## 2. 策略一览",
    ),
], required=False)


# --------------------------------------------------------------------------- #
# docs/02 · Opportunity 增加 match_score
# --------------------------------------------------------------------------- #
patch("docs/02-领域模型与数据模型.md", [
    (
        "    # 分数（D08 双分）\n    rule_score: float | None",
        "    # 分数（D08 双分）\n"
        "    match_score: float | None       # 匹配度，可解释、可复算（规格 §4.3 / §5.8）\n"
        "    rule_score: float | None",
    ),
])

print("\n全部文档已与实现对齐。")
